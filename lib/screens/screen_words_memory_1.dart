import 'package:flutter/material.dart';

class ScreenWordsMemory1 extends StatelessWidget {
  const ScreenWordsMemory1({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(''),
      ),
      body: Center(
        child: Text('ScreenWordsMemory1'),
      ),
    );
  }
}
